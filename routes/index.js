const {
    // authRouter, userRouter, noticesRouter, servicesRouter,
    newsRouter } = require("./api")

module.exports = {
    // authRouter,
    // userRouter,
    // noticesRouter,
    // servicesRouter,
    newsRouter
}